 Dataset → Filtering → Preprocessing → Normalization → AI Models

Implement using jupyter notebook Python

Pandas
NumPy
Scikit-learn

✅ Tasks:

Remove noise
Handle missing values
Normalize data
Prepare tabular format

📌 This runs on:

Local system (acceptable for demo)

Ai model

TTL – Irrigation Prediction
Model: Random Forest / LSTM
Input: soil moisture, temperature, humidity
Output: water needed (Yes / No or Liters)